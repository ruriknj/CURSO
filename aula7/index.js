const primeiroNumero = 5; //constantes sempre precisam ser inicializadas
const segundoNumero = 10; //não se pode mudar os valores das constantes
const resultado = primeiroNumero * segundoNumero;
const resultaDuplicado = resultado * 2; 
//const resultaDuplicado = resultado * 2;
console.log(resultaDuplicado);

console.log(typeof primeiroNumero); //saber o tipo da variável ou constante
